export {
  types as quickScanResultTypes,
  queries as quickScanResultQueries,
  mutations as quickScanResultMutations
} from "./schema";

import * as resolvers from "./resolvers";
export default resolvers;
